## Deprecated Sentinel Module

This `sentinel/` package is a legacy implementation kept for reference from earlier project sprints.

The live backend used by the current AURA demo is:

- `aura_backend/` for API and runtime orchestration
- `aura-frontend/` for the active dashboard UI

Do not treat `sentinel/` as the active runtime backend for the final demo.
